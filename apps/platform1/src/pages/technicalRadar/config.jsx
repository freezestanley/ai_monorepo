/**
 * 审批
 * @description wait: 待提交, approving: 待审批, pass: 通过 reject：不通过
 */
export const ApproveStatus = {
  wait: { color: "default" },
  approving: { color: "warning" },
  pass: { color: "success" },
  reject: { color: "error" }
}
